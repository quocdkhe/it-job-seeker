version: '3.8'

services:
  it-job-seeker:
    build: .
    ports:
      - "10000:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
